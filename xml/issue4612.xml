<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4612" status="New">
<title>Specify the details of `std::nullopt_t`</title>
<section><sref ref="[optional.nullopt]"/></section>
<submitter>Jiang An</submitter>
<date>11 Aug 2026</date>
<priority>99</priority>

<discussion>
<p>
LWG <iref ref="4497"/> specified that `std::nullopt_t` is copyable and three-way comparable. 
However, during implementing and testing its resolution in libc++, it was found that almost 
no intended operation is specified to be have desired property - being `noexcept` or trivial, 
or even `constexpr` for some special member functions. Maybe we should specify the details of 
`std::nullopt_t` more precisely.
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5054"/>.
</p>

<ol>
<li>
<p>Modify <sref ref="[optional.nullopt]"/> as indicated:</p>

<blockquote class="note">
<p>
[<i>Drafting note</i>: It will become redundant to specify that `nullopt_t` is empty, non-aggregate, 
and modeling certain concepts. In current implementations, `nullopt_t` does have some member type, 
so perhaps we shouldn't specify lack of member types.]
</p>
</blockquote>

<blockquote>
<pre>
struct nullopt_t{<del><i>see below</i></del><ins>
  // <i>unspecified constructors</i>

  friend strong_ordering operator&lt;=&gt;(nullopt_t, nullopt_t) = default;
</ins>};
inline constexpr nullopt_t nullopt(<i>unspecified</i>);
</pre>
<p>
-1- The struct `nullopt_t` is <del>an empty</del> class type used as a unique type to 
indicate the state of not containing a value for optional objects. In particular, 
<tt>optional&lt;T&gt;</tt> has a constructor with `nullopt_t` as a single
argument; this indicates that an optional object not containing a value shall be constructed.
<p/>
-2- Type `nullopt_t` does not have <del>a default constructor or an initializer-list constructor, 
and is not an aggregate. `nullopt_t` models `copyable` and 
<tt>three_way_comparable&lt;strong_ordering&gt;</tt></del><ins>base class, non-static data member, 
default constructor, initializer-list constructor, or explicitly declared non-static member 
function that is not a constructor. All special member functions of `nullopt_t` are implicitly 
declared</ins>.
</p>
</blockquote>
</li>

</ol>

</resolution>

</issue>
