<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd" [ 
  <!ENTITY nbsp "&#160;">
] >

<issue num="2383" status="Open">
<title>Overflow cannot be ill-formed for chrono::duration integer literals</title>
<section><sref ref="[time.duration.literals]"/></section>
<submitter>Jonathan Wakely</submitter>
<date>16 May 2014</date>
<priority>3</priority>

<discussion>
<p>
<sref ref="[time.duration.literals]"/> p3 says:
</p>
<blockquote><p>
If any of these suffixes are applied to an integer literal and the
resulting <tt>chrono::duration</tt> value cannot be represented in the result
type because of overflow, the program is ill-formed.
</p></blockquote>
<p>
Ill-formed requires a diagnostic at compile-time, but there is no way
to  detect the overflow from <tt>unsigned long long</tt> to the <tt>signed
duration&lt;&gt;::rep</tt> type.
<p/>
Overflow could be detected if the duration integer literals were
literal operator templates, otherwise overflow can either be undefined
or a run-time error, not ill-formed.
</p>

<note>Urbana 2014-11-07: Move to Open</note>

<note>2026-06-16; Jonathan adds wording.</note>
<p>
The library has no way to distinguish the case where a suffix is
"applied to an integer literal", as opposed to using function call notation
like `operator""s(123)`.
There is never overflow, because converting `unsigned long long` to a `rep`
such as `signed long long` is always well-formed, just not value-preserving.
Unless it is used as a constant subexpression, a user-defined-literal such as
`123s` is not constant evaluated, so in general checking the value at
compile-time is unimplementable.
</p>
<p>
If we made these operators `consteval` then using them as the suffix of a
<i>user-defined-literal</i> would always be a constant expression.
It would make it ill-formed to call `operator""s(some_variable)`
but I consider that perfectly acceptable. Such uses are absurd,
and the existing wording is all specified in terms of suffixes anyway,
not in terms of function calls to those operators.
</p>

</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5046"/>.
</p>

<blockquote class="note">
Two options are presented, choose only one.
</blockquote>
<strong>Option A</strong>
<ol>
<li>
<p>Modify <sref ref="[time.duration.literals]"/> as indicated:</p>
<blockquote>
<p>-1-
This subclause describes literal suffixes for constructing duration literals.
The suffixes `h`, `min`, `s`, `ms`, `us`, `ns` denote duration values of the
corresponding types `hours`, `minutes`, `seconds`, `milliseconds`,
`microseconds`, and `nanoseconds` respectively if they are applied to
<i>integer-literals</i>.
</p>
<p>-2-
If any of these suffixes <del>are</del> <ins>is</ins> applied to a
<i>floating-point-literal</i> the result is a `chrono::duration` literal
with an unspecified <i>floating-point</i> representation.
</p>
<p>-3-
If any of these suffixes <del>are</del> <ins>is</ins> applied to an
<i>integer-literal</i> and
<ins>
the parameter cannot be represented in the `rep` type of the return type,
</ins>
<del>
the resulting `chrono::duration` value cannot be
represented in the result type because of overflow,
</del>
the program is ill-formed.
</p>
<p>-4-
[<i>Example 1</i>: The following code shows some duration literals.
<blockquote><pre><code>using namespace std::chrono_literals;
auto constexpr aday=24h;
auto constexpr lesson=45min;
auto constexpr halfanhour=0.5h;
</code></pre></blockquote>
&mdash; <i>end example</i>]
</p>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::hours                                 operator""h(unsigned long long hours);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>, ratio&lt;3600, 1&gt;&gt; operator""h(long double hours);
</pre>
<blockquote>
<p>-5- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `hours` hours.</p>
</blockquote>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::minutes                             operator""min(unsigned long long minutes);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>, ratio&lt;60, 1&gt;&gt; operator""min(long double minutes);
</pre>
<blockquote>
<p>-6- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `minutes` minutes.</p>
</blockquote>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::seconds               operator""s(unsigned long long seconds);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>&gt; operator""s(long double seconds);
</pre>
<blockquote>
<p>-7- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `seconds` seconds.</p>
<p>-8-
[<i>Note 1</i>:
The same suffix `s` is used for `basic_string` but there is no conflict,
since duration suffixes apply to numbers and string literal suffixes apply
to character array literals.
&mdash; <i>end note</i>]
</p>
</blockquote>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::milliseconds                operator""ms(unsigned long long msec);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>, milli&gt; operator""ms(long double msec);
</pre>
<blockquote>
<p>-9- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `msec` milliseconds.</p>
</blockquote>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::microseconds                operator""us(unsigned long long usec);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>, micro&gt; operator""us(long double usec);
</pre>
<blockquote>
<p>-10- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `usec` microseconds.</p>
</blockquote>

<pre>
<del>constexpr</del> <ins>consteval</ins> chrono::nanoseconds                operator""ns(unsigned long long nsec);
<del>constexpr</del> <ins>consteval</ins> chrono::duration&lt;<i>unspecified</i>, nano&gt; operator""ns(long double nsec);
</pre>
<blockquote>
<p>-11- <i>Returns</i>: A `duration` <del>literal</del> <ins>value</ins> representing `nsec` nanoseconds.</p>
</blockquote>

</blockquote>
</li>

<li>
<p>Add <sref ref="[diff.cpp26.duration]"/> to Annex C:</p>
<blockquote>
<p><b>C.1.? Clause <sref ref="[time]"/>: Time library</b></p>
<p>
<ins><b>Affected subclause</b>: <sref ref="[time.duration.literals]"/></ins>
<p/>
<ins><b>Change</b>: Literal operators for `duration` literals are immediate
functions.</ins>
<p/>
<ins><b>Rationale</b>: Allow checking for overflow.</ins>
<p/>
<ins><b>Effect on original feature</b>: Valid C++ 2026 code that used
function call syntax and non-constant arguments to create duration literals
is ill-formed in this document.</ins>
</p>
<p><ins>[<i>Example ?</i>:</ins>
<blockquote><pre><code><ins>int n = 60;
auto t = std::chrono::literals::operator""s(n); // ill-formed; previously well-formed
</ins></code></pre></blockquote>
<ins>&mdash; <i>end example</i>]</ins>
</p>
</blockquote>
</li>
</ol>

<strong>Option B</strong>
<ol>
<li>
<p>Modify <sref ref="[time.duration.literals]"/> as indicated:</p>
<blockquote>
<p>-1-
This subclause describes literal suffixes for constructing duration literals.
The suffixes `h`, `min`, `s`, `ms`, `us`, `ns` denote duration values of the
corresponding types `hours`, `minutes`, `seconds`, `milliseconds`,
`microseconds`, and `nanoseconds` respectively if they are applied to
<i>integer-literals</i>.
</p>
<p>-2-
If any of these suffixes <del>are</del> <ins>is</ins> applied to a
<i>floating-point-literal</i> the result is a `chrono::duration` literal
with an unspecified <i>floating-point</i> representation.
</p>
<p>-3-
If any of these suffixes <del>are</del> <ins>is</ins> applied to an
<i>integer-literal</i> and
<ins>
the parameter cannot be represented in the `rep` type of the return type,
</ins>
<del>
the resulting `chrono::duration` value cannot be
represented in the result type because of overflow,
</del>
the program is ill-formed.
</p>
<p><ins>-?-
It is unspecified whether the literal operators below are usable only as
the suffix of a <i>user-defined-literal</i> (<sref ref="[lex.ext]"/>),
or whether they can also be called using function call notation.
If they can be called using function call notation, using `unsigned long long`
parameters that cannot be represented in the `rep` type of the return type
is ill-formed, no diagnostic required.
</ins>
</p>
</blockquote>
</li>
</ol>

</resolution>

</issue>
