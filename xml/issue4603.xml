<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4603" status="Ready">
<title>`layout_stride` should accept zero strides for empty `extents`</title>
<section>
<sref ref="[mdspan.layout.stride.cons]"/>
</section>
<submitter>Tomasz Kamiński</submitter>
<date>7 Jun 2026</date>
<priority>3</priority>

<discussion>
<p>
`layout_stride` constructor accepting `extents` and `strides` values,
requires that stride values are always greater than zero, even if the `extents`
represents empty multidimensional index space. As for such `extents` types
`layout_left` and `layout_right` mappings will result in zero strides,
leads to inconsistencies in behavior:
</p>
<pre>
  std::dextents&lt;size_t, 3&gt; ext{1, 0, 4};
  std::layout_left ll{ext};
  std::layout_stride ls{std::layout_left{ext}}; // OK, no precondition on constructor

  std::array&lt;size, 3&gt; strides{ll.stride(0), ll.stride(1), ll.stride(2)};
  std::layout_stride lse{ext, strides}; // UB, ll.stride(2) is zero
</pre>

<p>
As by definition any mapping over empty multidimensional index space
is unique, we should weaken that precondition and require non-zero
strides only for non-empty `extents`.
</p>

<p>
Furthermore, the current wording "result of converting `s[i]` to
`index_type` is greater than `0`", forces implementation to convert
the inputs to (mostly unsigned) `index_type` before the check,
preventing detection of negative values. We should use <tt><i>index-cast</i></tt>
instead.
</p>


<note>2026-07-10; LWG telecon.</note>
<p>
Set priority to 3 after telecon poll.
Tomasz to revise proposed resolution to incorporate other change from
<paper num="P3959R0"/>, which deals with this same issue.
</p>

<superseded>
<p>
This wording is relative to <paper num="N5046"/>.
</p>

<ol>

<li><p>Modify <sref ref="[mdspan.layout.stride.cons]"/> as indicated:</p>

<blockquote>
<pre>
template&lt;class OtherIndexType&gt;
  constexpr mapping(const extents_type&amp; e, span&lt;OtherIndexType, rank_&gt; s) noexcept;
template&lt;class OtherIndexType&gt;
  constexpr mapping(const extents_type&amp; e, const array&lt;OtherIndexType, rank_&gt;&amp; s) noexcept;
</pre>
<blockquote>
<p>-3- <i>Constraints</i>: [&hellip;]</p>
<p>-4- <i>Preconditions</i>:</p>
<ul style="list-style-type: none">
<li><p>(4.1) &mdash;
<del>The result of converting <tt>s[<i>i</i>]</tt> to <tt>index_type</tt> is greater than <tt>0</tt></del>
<ins><tt>extents_type::<i>index-cast</i>(s[<i>i</i>])</tt> is non-negative and representable as a value of
type <tt>index_type</tt> (<sref ref="[basic.fundamental]"/>)</ins> for all <tt><i>i</i></tt> in the range
<tt>[0, <i>rank_</i>)</tt>.</p></li>
<li><p>(4.2) &mdash; <tt><i>REQUIRED-SPAN-SIZE</i>(e, s)</tt> is representable as a value of type
<code>index_type</code> <del>(<sref ref="[basic.fundamental]"/>)</del>.</p></li>
<li><p>(4.3) &mdash; If <ins>both</ins> <tt><i>rank_</i></tt> <ins>and the size of
the multidimensional index space <tt>extents()</tt> are</ins><del>is</del> greater than <tt>0</tt>,
then there exists a permutation <tt><i>P</i></tt> of the integers in the range <tt>[0, <i>rank_</i>)</tt>,
such that <ins><tt>s[<i>p<sub>0</sub></i>] > 0</tt> is <tt>true</tt> and</ins> <tt>s[<i>p<sub>i</sub></i>] >=
s[<i>p<sub>i-1</sub></i>] * e.extent(<i>p<sub>i</sub></i>)</tt> is <tt>true</tt> for all <tt><i>i</i></tt>
in the range <tt>[1, <i>rank_</i>)</tt>, where <tt><i>p<sub><del>i</del><ins>k</ins></sub></i></tt> is the
<tt><i><del>i</del><ins>k</ins></i><sup>th</sup></tt> element of <tt><i>P</i></tt>.</p></li>
</ul>
<p>-5- <i>Effects</i>: [&hellip;]</p>
</blockquote>
</blockquote>

</li>
</ol>
</superseded>

<note>2026-07-10; Tomasz provides updated wording.</note>

<note>2026-07-17 LWG telecon; Change status New &rarr; Ready.</note>

</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5046"/>.
</p>

<ol>

<li><p>Modify <sref ref="[mdspan.layout.stride.cons]"/> as indicated:</p>

<blockquote>
<pre>
template&lt;class OtherIndexType&gt;
  constexpr mapping(const extents_type&amp; e, span&lt;OtherIndexType, rank_&gt; s) noexcept;
template&lt;class OtherIndexType&gt;
  constexpr mapping(const extents_type&amp; e, const array&lt;OtherIndexType, rank_&gt;&amp; s) noexcept;
</pre>
<blockquote>
<p>-3- <i>Constraints</i>: [&hellip;]</p>
<p>-4- <i>Preconditions</i>:</p>
<ul style="list-style-type: none">
<li><p>(4.1) &mdash;
<del>The result of converting <tt>s[<i>i</i>]</tt> to <tt>index_type</tt> is greater than <tt>0</tt></del>
<ins><tt>extents_type::<i>index-cast</i>(s[<i>i</i>])</tt> is non-negative and representable as a value of
type <tt>index_type</tt> (<sref ref="[basic.fundamental]"/>)</ins> for all <tt><i>i</i></tt> in the range
<tt>[0, <i>rank_</i>)</tt>.</p></li>
<li><p>(4.2) &mdash; <tt><i>REQUIRED-SPAN-SIZE</i>(e, s)</tt> is representable as a value of type
<code>index_type</code> <del>(<sref ref="[basic.fundamental]"/>)</del>.</p></li>
<li><p>(4.3) &mdash; If <ins>both</ins> <tt><i>rank_</i></tt> <ins>and the size of
the multidimensional index space <tt>extents()</tt> are</ins><del>is</del> greater than <tt>0</tt>,
then there exists a permutation <tt><i>P</i></tt> of the integers in the range <tt>[0, <i>rank_</i>)</tt>,
such that <ins><tt>s[<i>p<sub>0</sub></i>] > 0</tt> is <tt>true</tt> and</ins> <tt>s[<i>p<sub>i</sub></i>] >=
s[<i>p<sub>i-1</sub></i>] * e.extent(<i>p<sub>i</sub></i>)</tt> is <tt>true</tt> for all <tt><i>i</i></tt>
in the range <tt>[1, <i>rank_</i>)</tt>, where <tt><i>p<sub><del>i</del><ins>k</ins></sub></i></tt> is the
<tt><i><del>i</del><ins>k</ins></i><sup>th</sup></tt> element of <tt><i>P</i></tt>.</p></li>
</ul>
<p>-5- <i>Effects</i>: [&hellip;]</p>
</blockquote>
</blockquote>

<blockquote>
<pre>
template&lt;class StridedLayoutMapping&gt;
  constexpr explicit(see below)
    mapping(const StridedLayoutMapping&amp; other) noexcept;
</pre>
<blockquote>
<p>-6- <i>Constraints</i>: [&hellip;]</p>
<p>-7- <i>Preconditions</i>:</p>
<ul style="list-style-type: none">
<li><p>(7.1) &mdash; <tt>StridedLayoutMapping</tt> meets the layout mapping requirements
(<sref ref="[mdspan.layout.reqmts]"/>),</p></li>
<li><p><del>(7.2) &mdash; <tt>other.stride(<i>r</i>) > 0</tt> is true for every rank index
<i>r</i> of <code>extents()</code>,</del></p></li>
<li><p>(7.3) &mdash; <tt>other.required_span_size()</tt> is representable as a value of
type <tt>index_type</tt> (<sref ref="[basic.fundamental]"/>), and,</p></li>
<li><p>(7.4) &mdash; <tt><i>OFFSET</i>(other) == 0</tt> is <tt>true</tt>.</p></li>
</ul>
<p>-5- <i>Effects</i>: [&hellip;]</p>
</blockquote>
</blockquote>

</li>
</ol>
</resolution>

</issue>
