<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4618" status="New">
<title>Unclear effects in insertion and extraction of random number engines and random number distributions</title>
<section><sref ref="[rand.req.eng]"/><sref ref="[rand.req.dist]"/></section>
<submitter>Jiang An</submitter>
<date>30 Aug 2026</date>
<priority>99</priority>

<discussion>
<p>
In <sref ref="[rand.req.eng]"/> and <sref ref="[rand.req.dist]"/>, <tt>operator&lt;&lt;</tt> and 
<tt>operator&gt;&gt;</tt> are specified with <tt><i>fmtflags</i></tt>, which looks like an exposition-only 
data member but doesn't actually exist in the synopses. Presumably, we should use `flags()` instead and 
specify the function calls that change flags. Additionally, Postconditions should perhaps say "restored" 
instead of "unchanged".
<p/>
Moreover, <sref ref="[rand.req.eng]"/> p7 says "space character". But its meaning is unclear when `charT` 
is not a character type but a valid character container type, assuming the implementation allows 
instantiating `basic_ostream` with such a type.
</p>
</discussion>

<resolution>
</resolution>

</issue>
